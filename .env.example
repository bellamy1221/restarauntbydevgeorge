# Site URL (optional). On Vercel, leave empty to use the deployment host automatically.
NEXT_PUBLIC_SITE_URL=

# Required for reservation → Telegram (server-side only, never NEXT_PUBLIC_)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
